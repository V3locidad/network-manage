# Copie en .env (ignoré par git).
#
# WEBSSH_HASH = hash du mot de passe d'accès au terminal WebSSH. Génère-le :
#   docker run --rm caddy caddy hash-password --plaintext 'TON_MOT_DE_PASSE'
# Colle la valeur (commence par $2a$...) en DOUBLANT chaque $ -> $$.
WEBSSH_HASH=
