# (Plus de variable nécessaire ici.)
#
# Le terminal WebSSH (:8443) est désormais protégé par le MÊME login que
# l'interface web, via `forward_auth` dans le Caddyfile (SSO). Il n'y a donc
# plus de mot de passe `basic_auth` distinct ni de WEBSSH_HASH à générer.
